# Cursor Rules

Rules in this directory are mandatory for all AI assistants working on this repo.

## Layout

- **Subdirs:** Rules from MathematicalAutopsy: `collaboration/`,
  `documentation/`, `quality/`, `development/`, `ma-process/`, `testing/`,
  `workflow/`.

## Governance paths

Some rules reference `docs/governance/`. In this project, canonical governance
docs may be at **project root** or **`docs/governance/`** or **`ops/docs/`**.
Apply the same intent when following those rules.
